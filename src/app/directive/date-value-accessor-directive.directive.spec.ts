import { DateValueAccessorDirectiveDirective } from './date-value-accessor-directive.directive';

describe('DateValueAccessorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DateValueAccessorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
